import React from 'react';

import ResortList from '../components/ResortList';

const ResortListContainer = ({ regions }) => (
  <ResortList regions={ regions } />
);

export default ResortListContainer;
